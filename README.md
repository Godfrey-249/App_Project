# App_Project
This is a repository with an app to help business owners connect with suppliers and workers.

This is a prototype of a JS project with a python backend deployed using streamlit. 

The project requiers a lot of libraries, as in the requirements.txt and a database created using SQL.

